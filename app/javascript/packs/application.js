import "bootstrap";

const filterCoaches = () => {
  const speFilter = document.querySelector("#spe-filter");
  const expFilter = document.querySelector("#exp-filter");
  const minFilter = document.querySelector("#min-filter");
  const maxFilter = document.querySelector("#max-filter");
  const filtersInput = document.querySelector(.filter)
  speFilter.addEventListener("blur", {
    // Faire le tri dans les coaches
  });
}

 export { filterCoaches };
